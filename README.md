The project currently has a functional back end with springboot integrated into the java classes. the front end is functional but separate until we set up springboot controllers.
current primary objectives:
-set up springboot controllers
-add more administrator functionalities
-update front end UI
